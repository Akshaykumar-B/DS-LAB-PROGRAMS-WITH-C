#include <stdio.h>
int main(){
    int n,a=0,b=1,c,i,sum=1;
    printf("Enter n: "); scanf("%d",&n);
    if(n==1) sum=0;
    for(i=2;i<n;i++){ c=a+b; sum+=c; a=b; b=c; }
    printf("Sum=%d\n",sum);
}
